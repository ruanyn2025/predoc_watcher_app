# 网页界面本身（app.py）只需要 jinja2，其余用标准库。
jinja2>=3.0

# 自带抓取，不依赖邮件推送版。
requests>=2.25
beautifulsoup4>=4.9
certifi          # fetch.py 直接用它来补 predoc.org 缺失的中间证书

# 桌面版（desktop.py）：窗口 + 系统托盘 + 图标生成。
# 只跑网页版的话这三个不装也行。
pywebview>=4.0
pystray>=0.19
pillow>=9.0
